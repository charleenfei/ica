version: "3"

services:
  build-icad:
    build:
      context: .
      dockerfile: Dockerfile
    image: icad:local

  init-chain:
    image: icad:local
    # command: bash ./network/init.sh
    command: tail -f /dev/null
    # volumes:
    #   - ./storage:/data
